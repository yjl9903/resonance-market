<script setup lang="ts">
import { Dialog, DialogTrigger } from './ui/dialog';
import Button from './ui/button/Button.vue';

const registerIsOpen = ref(false);
</script>
<template>
  <Dialog v-model="registerIsOpen">
    
    </DialogTrigger>
  </Dialog>
</template>
